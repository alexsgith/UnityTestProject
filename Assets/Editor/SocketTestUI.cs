using UnityEditor;
using UnityEngine;
//[CustomEditor(typeof(SocketTest))]
public class SocketTestUI : Editor
{
    /*public override void OnInspectorGUI()
    {
        DrawDefaultInspector();
        SocketTest socketTest = (SocketTest)target;
        if (GUILayout.Button("Emit Message"))
        {
            socketTest.EmitMessage();
            Debug.Log("button clicked");
        }
    }*/
}
